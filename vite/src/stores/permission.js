import { reactive } from 'vue';
import { defineStore } from 'pinia'

export const usePermission = defineStore('permissiion', () => {
  const permissiion = reactive({

  })


  return { permissiion }
})
